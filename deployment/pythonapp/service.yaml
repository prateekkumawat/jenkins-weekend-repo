apiVersion: v1
kind: Service
metadata:
  name: pythonapp-svc
  namespace: pythonapp
  labels:
    app: python-app
spec:
  selector:
    app: python-app
  type: ClusterIP 
  ports:
  - port: 5003
    targetPort: 5000
