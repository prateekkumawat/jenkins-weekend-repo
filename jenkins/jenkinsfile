pipeline {
    agent {
        label 'cimachine'
    }
    stages{
        stage("AppCode Checkout"){
            steps{
                git branch:'learning-app-jenkins', url:'https://github.com/prateekkumawat/jenkins-weekend-repo.git'
            }
        }
        stage("AppImage Create"){
            steps {
                dir("web1/"){ 
                  sh " docker image build -t app:ubi . "
                }
            }
        }
          stage("VillaImage Create"){
            steps {
                dir("web2/"){ 
                  sh " docker image build -t villa:ubi . "
                }
            }
        }
        stage("Deploy container"){
            steps{
                sh "docker-compose up -d "
            }
        }
    }
}